서버는 다중 쓰레드로 구현, 새로운 사용자가 올 때마다 새로운 키를 생성해서 할당.

처음 서버 생성에서는 별도의 gui가 없고 사용자가 암호화된 평문을 보내면 서버측에서 사용자마다 별도의 gui 생성

파일 암호화, 복호화는 파일로부터 스트링을 읽어들여 서버에 스트링 형태로 넘기고 그 것을 복호화.

클라이언트 측에서 파일로 부터 스트링을 얻어왔다면 서버로 flag를 보내어 파일인지 구분하고 복호화된 내용을 파일에 붙여씀(append)

각종 TextArea가 입력되지 않았을 때 버튼 이벤트 발생 등 여러 예외 처리

p, q는 소수로 고정, e와 d는 랜덤한 변수로 조건에 맞게 생성.

한글로 이루어진 String은 UTF방식이 아니여서 깨져서 보임

별도의 라이브러리를 사용하지 않고 명세와 동일한 계산방식으로 구현

나머지 명세와 동일.

한 학기 수고 많으셧습니다.
